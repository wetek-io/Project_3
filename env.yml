name: project_3
channels:
  - defaults
  - conda-forge
dependencies:
  - python=3.12  # Match the version you're using
  - numpy
  - pytorch
  - torchvision
  - matplotlib
  - pillow
  - opencv
  - pandas  # Optional if working with datasets
  - pip
  - pip:
      - torch  # For PyTorch-based deep learning
      - matplotlib  # For visualization
      - torchsummary  # Optional: For summarizing your model architecture